class Item {
    unit: string
    price: number
    name: string
    barcode: string

    constructor(unit: string, price: number, name: string, barcode: string) {
      this.unit = unit
      this.price = price
      this.name = name
      this.barcode = barcode
    }
}
